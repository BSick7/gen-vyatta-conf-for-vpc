### Running from command line

```
USAGE: node index.js [OPTION1] [OPTION2]... arg1 arg2...
The following options are supported:
 -l, --local_subnet    Vyatta Local subnet (mandatory)
 -r, --remote_subnet   Vyatta Remote subnet (mandatory)
 -f, --interface       Vyatta Interface device
 -i, --input           Input filepath
 -o, --output          Output filepath

```